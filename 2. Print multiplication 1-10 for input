void main (){
//Print multiplication 1-10 for input
  print("Input number to multiply: ");
  int input = int.parse(stdin.readLineSync(encoding: Encoding.getByName("UTF-8")));
  for(int i = 1; i <= 10; i++){
    print('$input * $i = ${input*i}');
  }
}
